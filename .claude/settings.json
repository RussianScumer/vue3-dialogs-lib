{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath'); case \"$f\" in *.ts|*.vue) node_modules/.bin/eslint --fix --no-warn-ignored \"$f\" 2>/dev/null || true;; esac",
            "timeout": 30,
            "statusMessage": "eslint --fix"
          }
        ]
      }
    ]
  }
}
